import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const bookingTicketReducer = createSlice({
  name: "bookingTicketReducer",
  initialState,
  reducers: {},
});

export const {} = bookingTicketReducer.actions;

export default bookingTicketReducer.reducer;
