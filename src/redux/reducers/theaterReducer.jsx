import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const theaterReducer = createSlice({
  name: "theaterReducer",
  initialState,
  reducers: {},
});

export const {} = theaterReducer.actions;

export default theaterReducer.reducer;
